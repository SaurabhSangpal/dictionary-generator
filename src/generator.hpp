#pragma once

/* Generates the word list
** Returns a vector
*/

#include <iostream>
#include <fstream>
#include <string>
#include <vector>

std::vector<std::string> generator(std::vector<std::string>& x);